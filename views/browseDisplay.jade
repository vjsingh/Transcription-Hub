.row.content-box
  .browse-pane#browse(height='600px !important')
    - for (var sIndex in browseItems)
      - s = browseItems[sIndex]
      br
      a(href=s.url)
        = s.value
        | &nbsp(#{s.count})
